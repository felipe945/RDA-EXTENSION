# FIX_AUDIT_T4 — Summary (Extension: Sidepanel, Palette & Packaging)

`CHECKPOINT_T4_DONE: b8797dd` — palette unified, card hierarchy fixed, twin-row gone,
scripts search live, calls authed via SW, packed to **2.10.0**.

**Files changed (owned):** `sidepanel.js` · `sidepanel.css` · `styles.css` · `background.js` ·
`manifest.json` · `sidepanel.html` / `popup.html` / `popup.js` (logo hex + F3 note — unowned by
any other terminal) · pack outputs. The commit also carries T3's finished `instagram.js`
(CHECKPOINT_T3_DONE said "T4 may pack") + the stale-file deletions already in the tree, so the
zips match committed source. **`instagram.js` itself was not edited by T4.**

## Task 1 — C1: palette unified ✅
- `--pink: #FF3A69` is the single canonical token in `styles.css:root`, plus `--pink-rgb`
  so every alpha tint (`rgba(var(--pink-rgb),.x)`) follows a one-line token change.
- `--purple` deleted; **zero** purple anywhere: `grep -n "7c3aed\|8b5cf6\|--purple" *.js *.css`
  → empty (also swept uppercase strays `#8B5CF6` avatar gradient, `#A78BFA` replied color,
  `#c4b5fd/#e9d5ff/#2a1f38/#241a38`). Folded: Replied/AI/reply accents → pink; 🌙 Late
  toggle + calendar semantics → blue.
- Logo mark + "FanMas" wordmark: `#F1567A` → `#FF3A69` in `sidepanel.html` ×3 + `popup.html`
  (one pink everywhere; logo's blue half `#8FBBFB` kept). `stageColor("Replied")` → `#FF3A69`.
- Update-banner restyled purple → pink tint.

## Task 2 — V1: button hierarchy ✅
- ONE pink filled button on the outreach card: **Open IG + Copy** (`btn-open-primary` now pink,
  was `#7c3aed`). Generate-opener → pink outline.
- DM Sent (was green outline), 🕐 Times, 📅 Book → quiet slate `styles.css` classes
  (`.btn-sent`, `.btn-row-sm`); inline hexes removed from `sidepanel.js`; the pink
  `#bookCalBtn { !important }` override block deleted. DQ undo now swaps classes, not cssText.
- **Red only for DQ** (`.btn-dq`).

## Task 3 — V4: twin day-rows killed ✅
- `fu-btn` row (+1d/+3d/+7d) + `fuClearBtn` + handlers + CSS deleted; **Snooze kept** as the one
  "later" control. `due_at` still auto-set (+3d) on DM Sent (sidepanel handler + background
  FB_DM_SENT), so Follow-ups tab keeps working. Read-only "due in Nd" label retained.

## Task 4 — F2: search-first scripts ✅
- `#scriptSearch` input above the category select; a query searches **all** categories by
  label+text and shows `Label · Category`; select scopes only when the query is empty;
  empty-state on no match.

## Task 5 — V2: shared tokens ✅
- Navy-slate set centralized as `--slate-bg/-card/-border/-border2/-text/-text2` in
  `styles.css:root`, matching T3's `FIX_AUDIT_DESIGN_TOKENS.md` exactly; note updated with the
  CSS var names, the logo decision, and the SW message contracts.

## Task 6 — F3 + auth-align ✅ (CHECKPOINT_T1_AUTH consumed)
- **D1 honored:** new `background.js` handlers `FETCH_OPENER` + `RESEARCH_LEAD` call
  `/api/opener` / `/api/ai/research-lead` from the SW with `Authorization: Bearer <fb_rep_token>`
  (Origin = `chrome-extension://<id>`, on T1's CORS allowlist). Sidepanel's Generate Opener now
  goes through `RESEARCH_LEAD`.
- **Verified vs T1's dev server (:3111):** both endpoints **401** bare; with a locally-minted
  repToken both pass the gate (research-lead 400 "Lead not found" on a bogus id as designed;
  opener reached generation and 500'd only because local env lacks an Anthropic key).
- **F3:** popup now prints "Replies are detected while you're viewing Instagram DMs" under the
  "N replies" stat.

## Task 7 — bump + pack ✅
- `manifest.json` → **2.10.0**; `node --check` all 7 shipped JS files pass.
- `pack:ext` + `pack:webstore` clean; `outreach-queue.js` regenerated by the drift guard (not
  hand-edited); `public/extension/fanbasis-extension.zip` + `latest.json` committed.
  `dist/fanbasis-extension-webstore.zip` rebuilt at 2.10.0 (gitignored — hand to Web Store).

## Verification
- Playwright (real Chrome, chrome.* stub, `sidepanel.html`): **18/18** — one pink-filled button
  on the card, quiet secondaries, red only on DQ, no `.fu-btn`/"Follow up:" row, 3 Snooze
  buttons, generate-opener pink outline, search across categories + empty state + restore,
  zero purple in computed styles. Screenshot: scratchpad `pw/sidepanel-outreach.png`.

## ⚠ Flag for next wave (T3-owned file)
`instagram.js:~1651` still fetches `/api/opener` **directly from the content script** — that
request carries `Origin: https://www.instagram.com`, which T1's CORS allowlist rejects, so the
IG card's AI-opener fetch will fail closed (falls back to template scripts with a "⚠ AI failed"
badge — not silent). One-line fix: swap the fetch for
`chrome.runtime.sendMessage({ type: "FETCH_OPENER", params: { channel, ig_username, name, bio, followers, lead_id } })`
— the SW handler is live. Also noted in `FIX_AUDIT_DESIGN_TOKENS.md`.

## Needs Felipe
- **Upload `dist/fanbasis-extension-webstore.zip` (v2.10.0) to the Chrome Web Store** —
  extension changes don't ship on push.
- D-brand: `#FF3A69` adopted everywhere incl. the logo (brief default). If you prefer
  `#F1567A`, it's now literally one token edit (`styles.css:12`) + the two logo SVG hexes.
- F3 roadmap: approve a server-side reply producer (Gmail watch + IG poll) as the next wave —
  all three audits call this the core-loop hole.
